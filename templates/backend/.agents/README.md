# Agent Guide Entry Point

Use `.agents/` as the local instruction hub for project updates.

Read in this order before editing:

1. `.agents/workflow/README.md`
2. `.agents/skills/README.md`

Simple explanation:
- `workflow` tells you the step-by-step process.
- `skills` tells you who owns which part of the code.

Keep these files updated whenever the project structure changes.
